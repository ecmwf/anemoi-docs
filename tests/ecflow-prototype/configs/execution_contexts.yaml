
execution_contexts:
    defaults:
        queue: nf
        memory_per_task: 8Gb
        job_name: "%FAMILY1:NOT_DEF%_%TASK%"
        tmpdir: 20Gb
    parallel:
        tasks: 32
        memory_per_task: 4Gb
    single_gpu:
        gpus: 1
