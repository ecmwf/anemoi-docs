# Configuration file for pyflow suite.

# -------- Suite deployment configuration -------------------------------------
# host options
suite_name: "anemoi_test"
hostname: "hpc"
user: "{USER}"

# server options
deploy_dir: "{PERM}/pyflow/anemoi_test"
job_out: "%ECF_HOME%"
workdir: "$TMPDIR"

output_root: "{SCRATCH}/anemoi_test/output"
data_dir: "{SCRATCH}/anemoi_test/data"

# ---------- Specific optionals -----------------------------------------------
# add your project's specific variables and options here

# TODO add options for the params to be changed by the github action, like versions/branches etc., plus paths to artifact dirs (of logs)
# look at build_options in Julien's workflow
